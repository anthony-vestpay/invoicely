{
  "name": "invoicely",
  "version": "0.1.0",
  "private": true,
  "description": "A lightweight SQLite invoice tracker POC",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test tests/**/*.test.js"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "express": "^4.21.2"
  }
}
