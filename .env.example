# Tax rate applied to invoice subtotals, as a percent (e.g. 8.5 for 8.5%).
# Ops can change this and restart the server without a code deploy.
# If unset, invoices are created with 0% tax.
TAX_RATE=0
